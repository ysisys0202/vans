"use strict";

const mainSlider = tns({
  container: ".main-slide-list",
  items: 1,
  mode: "gallery",
  autoplay: true,
  autoplayTimeout: 7000,
  controls: false,
  nav: false,
  autoplayButtonOutput: false,
});
